diff --git a/cuda_rasterizer/forward.cu b/cuda_rasterizer/forward.cu
index c5e01dd..81cc6db 100644
--- a/cuda_rasterizer/forward.cu
+++ b/cuda_rasterizer/forward.cu
@@ -13,6 +13,7 @@
 #include "auxiliary.h"
 #include <cooperative_groups.h>
 #include <cooperative_groups/reduce.h>
+#include <stdexcept>
 namespace cg = cooperative_groups;
 
 // Forward method for converting the input spherical harmonics
@@ -422,10 +423,130 @@ void FORWARD::render(
 		n_contrib,
 		bg_color,
 		out_color,
-		depths, 
+		depths,
 		depth);
 }
 
+// Top-K blending weights kernel. Same alpha-blending traversal as renderCUDA,
+// but instead of accumulating color we keep the K largest w_i = alpha_i * T_i
+// seen for each pixel, sorted descending. Templated on K so the topk array
+// lives in registers.
+template <uint32_t K>
+__global__ void __launch_bounds__(BLOCK_X * BLOCK_Y)
+renderTopKWeightsCUDA(
+	const uint2* __restrict__ ranges,
+	const uint32_t* __restrict__ point_list,
+	int W, int H,
+	const float2* __restrict__ points_xy_image,
+	const float4* __restrict__ conic_opacity,
+	float* __restrict__ top_weights)
+{
+	auto block = cg::this_thread_block();
+	uint32_t horizontal_blocks = (W + BLOCK_X - 1) / BLOCK_X;
+	uint2 pix_min = { block.group_index().x * BLOCK_X, block.group_index().y * BLOCK_Y };
+	uint2 pix = { pix_min.x + block.thread_index().x, pix_min.y + block.thread_index().y };
+	uint32_t pix_id = W * pix.y + pix.x;
+	float2 pixf = { (float)pix.x, (float)pix.y };
+
+	bool inside = pix.x < W && pix.y < H;
+	bool done = !inside;
+
+	uint2 range = ranges[block.group_index().y * horizontal_blocks + block.group_index().x];
+	const int rounds = ((range.y - range.x + BLOCK_SIZE - 1) / BLOCK_SIZE);
+	int toDo = range.y - range.x;
+
+	__shared__ int collected_id[BLOCK_SIZE];
+	__shared__ float2 collected_xy[BLOCK_SIZE];
+	__shared__ float4 collected_conic_opacity[BLOCK_SIZE];
+
+	float T = 1.0f;
+	float topk[K];
+	#pragma unroll
+	for (int k = 0; k < (int)K; ++k) topk[k] = 0.0f;
+
+	for (int i = 0; i < rounds; i++, toDo -= BLOCK_SIZE)
+	{
+		int num_done = __syncthreads_count(done);
+		if (num_done == BLOCK_SIZE)
+			break;
+
+		int progress = i * BLOCK_SIZE + block.thread_rank();
+		if (range.x + progress < range.y)
+		{
+			int coll_id = point_list[range.x + progress];
+			collected_id[block.thread_rank()] = coll_id;
+			collected_xy[block.thread_rank()] = points_xy_image[coll_id];
+			collected_conic_opacity[block.thread_rank()] = conic_opacity[coll_id];
+		}
+		block.sync();
+
+		for (int j = 0; !done && j < min(BLOCK_SIZE, toDo); j++)
+		{
+			float2 xy = collected_xy[j];
+			float2 d = { xy.x - pixf.x, xy.y - pixf.y };
+			float4 con_o = collected_conic_opacity[j];
+			float power = -0.5f * (con_o.x * d.x * d.x + con_o.z * d.y * d.y) - con_o.y * d.x * d.y;
+			if (power > 0.0f)
+				continue;
+			float alpha = min(0.99f, con_o.w * exp(power));
+			if (alpha < 1.0f / 255.0f)
+				continue;
+			float test_T = T * (1 - alpha);
+			if (test_T < 0.0001f)
+			{
+				done = true;
+				continue;
+			}
+
+			float w_i = alpha * T;
+			// Insert w_i into top-k buffer (kept sorted descending).
+			if (w_i > topk[K - 1])
+			{
+				topk[K - 1] = w_i;
+				#pragma unroll
+				for (int k = (int)K - 1; k > 0; --k)
+				{
+					if (topk[k] > topk[k - 1])
+					{
+						float tmp = topk[k];
+						topk[k] = topk[k - 1];
+						topk[k - 1] = tmp;
+					}
+				}
+			}
+
+			T = test_T;
+		}
+	}
+
+	if (inside)
+	{
+		#pragma unroll
+		for (int k = 0; k < (int)K; ++k)
+			top_weights[k * H * W + pix_id] = topk[k];
+	}
+}
+
+void FORWARD::render_topk_weights(
+	const dim3 grid, dim3 block,
+	int K,
+	const uint2* ranges,
+	const uint32_t* point_list,
+	int W, int H,
+	const float2* points_xy_image,
+	const float4* conic_opacity,
+	float* top_weights)
+{
+	switch (K)
+	{
+	case 1: renderTopKWeightsCUDA<1> << <grid, block >> > (ranges, point_list, W, H, points_xy_image, conic_opacity, top_weights); break;
+	case 2: renderTopKWeightsCUDA<2> << <grid, block >> > (ranges, point_list, W, H, points_xy_image, conic_opacity, top_weights); break;
+	case 4: renderTopKWeightsCUDA<4> << <grid, block >> > (ranges, point_list, W, H, points_xy_image, conic_opacity, top_weights); break;
+	case 8: renderTopKWeightsCUDA<8> << <grid, block >> > (ranges, point_list, W, H, points_xy_image, conic_opacity, top_weights); break;
+	default: throw std::runtime_error("render_topk_weights: only K in {1,2,4,8} is supported");
+	}
+}
+
 void FORWARD::preprocess(int P, int D, int M,
 	const float* means3D,
 	const glm::vec3* scales,
diff --git a/cuda_rasterizer/forward.h b/cuda_rasterizer/forward.h
index 5b18005..b7a7387 100644
--- a/cuda_rasterizer/forward.h
+++ b/cuda_rasterizer/forward.h
@@ -63,6 +63,18 @@ namespace FORWARD
 		float* out_color,
 		float* depths,
 		float* depth);
+
+	// Per-pixel top-K blending weights (w_i = alpha_i * T_i),
+	// kept sorted in descending order. Writes a [K, H, W] buffer.
+	void render_topk_weights(
+		const dim3 grid, dim3 block,
+		int K,
+		const uint2* ranges,
+		const uint32_t* point_list,
+		int W, int H,
+		const float2* points_xy_image,
+		const float4* conic_opacity,
+		float* top_weights);
 }
 
 
diff --git a/cuda_rasterizer/rasterizer.h b/cuda_rasterizer/rasterizer.h
index 64afdee..3ca52af 100644
--- a/cuda_rasterizer/rasterizer.h
+++ b/cuda_rasterizer/rasterizer.h
@@ -54,6 +54,31 @@ namespace CudaRasterizer
 			int* radii = nullptr,
 			bool debug = false);
 
+		// Forward pass that produces, for each pixel, the K largest
+		// alpha-blending weights (w_i = alpha_i * T_i), sorted descending.
+		// top_weights buffer is shape [K, H, W], row-major.
+		static int forward_topk_weights(
+			std::function<char* (size_t)> geometryBuffer,
+			std::function<char* (size_t)> binningBuffer,
+			std::function<char* (size_t)> imageBuffer,
+			const int P, int D, int M, int K,
+			const int width, int height,
+			const float* means3D,
+			const float* opacities,
+			const float* scales,
+			const float scale_modifier,
+			const float* rotations,
+			const float* cov3D_precomp,
+			const float* viewmatrix,
+			const float* projmatrix,
+			const float* cam_pos,
+			const float tan_fovx, float tan_fovy,
+			const bool prefiltered,
+			float* top_weights,
+			bool antialiasing,
+			int* radii = nullptr,
+			bool debug = false);
+
 		static void backward(
 			const int P, int D, int M, int R,
 			const float* background,
diff --git a/cuda_rasterizer/rasterizer_impl.cu b/cuda_rasterizer/rasterizer_impl.cu
index 41c4ed3..95eeab9 100644
--- a/cuda_rasterizer/rasterizer_impl.cu
+++ b/cuda_rasterizer/rasterizer_impl.cu
@@ -340,6 +340,138 @@ int CudaRasterizer::Rasterizer::forward(
 	return num_rendered;
 }
 
+// Forward pass that, per pixel, returns the top-K alpha-blending weights
+// (w_i = alpha_i * T_i), sorted descending. Reuses the standard preprocess +
+// tile-binning pipeline; only the final render kernel differs. No SH eval and
+// no color buffer are needed, so we pass colors_precomp as a non-null sentinel
+// (a temporary buffer we never read) to satisfy the preprocess invariants.
+int CudaRasterizer::Rasterizer::forward_topk_weights(
+	std::function<char* (size_t)> geometryBuffer,
+	std::function<char* (size_t)> binningBuffer,
+	std::function<char* (size_t)> imageBuffer,
+	const int P, int D, int M, int K,
+	const int width, int height,
+	const float* means3D,
+	const float* opacities,
+	const float* scales,
+	const float scale_modifier,
+	const float* rotations,
+	const float* cov3D_precomp,
+	const float* viewmatrix,
+	const float* projmatrix,
+	const float* cam_pos,
+	const float tan_fovx, float tan_fovy,
+	const bool prefiltered,
+	float* top_weights,
+	bool antialiasing,
+	int* radii,
+	bool debug)
+{
+	const float focal_y = height / (2.0f * tan_fovy);
+	const float focal_x = width / (2.0f * tan_fovx);
+
+	size_t chunk_size = required<GeometryState>(P);
+	char* chunkptr = geometryBuffer(chunk_size);
+	GeometryState geomState = GeometryState::fromChunk(chunkptr, P);
+
+	if (radii == nullptr)
+		radii = geomState.internal_radii;
+
+	dim3 tile_grid((width + BLOCK_X - 1) / BLOCK_X, (height + BLOCK_Y - 1) / BLOCK_Y, 1);
+	dim3 block(BLOCK_X, BLOCK_Y, 1);
+
+	size_t img_chunk_size = required<ImageState>(width * height);
+	char* img_chunkptr = imageBuffer(img_chunk_size);
+	ImageState imgState = ImageState::fromChunk(img_chunkptr, width * height);
+
+	// We need preprocess to skip the SH->RGB conversion (it only writes to
+	// geomState.rgb when colors_precomp is null). Pass geomState.rgb itself
+	// as colors_precomp so the SH branch is skipped; we never read this buffer.
+	const float* colors_precomp_dummy = geomState.rgb;
+	const float* shs_dummy = nullptr;
+
+	CHECK_CUDA(FORWARD::preprocess(
+		P, D, M,
+		means3D,
+		(glm::vec3*)scales,
+		scale_modifier,
+		(glm::vec4*)rotations,
+		opacities,
+		shs_dummy,
+		geomState.clamped,
+		cov3D_precomp,
+		colors_precomp_dummy,
+		viewmatrix, projmatrix,
+		(glm::vec3*)cam_pos,
+		width, height,
+		focal_x, focal_y,
+		tan_fovx, tan_fovy,
+		radii,
+		geomState.means2D,
+		geomState.depths,
+		geomState.cov3D,
+		geomState.rgb,
+		geomState.conic_opacity,
+		tile_grid,
+		geomState.tiles_touched,
+		prefiltered,
+		antialiasing
+	), debug)
+
+	CHECK_CUDA(cub::DeviceScan::InclusiveSum(geomState.scanning_space, geomState.scan_size, geomState.tiles_touched, geomState.point_offsets, P), debug)
+
+	int num_rendered;
+	CHECK_CUDA(cudaMemcpy(&num_rendered, geomState.point_offsets + P - 1, sizeof(int), cudaMemcpyDeviceToHost), debug);
+
+	size_t binning_chunk_size = required<BinningState>(num_rendered);
+	char* binning_chunkptr = binningBuffer(binning_chunk_size);
+	BinningState binningState = BinningState::fromChunk(binning_chunkptr, num_rendered);
+
+	duplicateWithKeys << <(P + 255) / 256, 256 >> > (
+		P,
+		geomState.means2D,
+		geomState.depths,
+		geomState.point_offsets,
+		binningState.point_list_keys_unsorted,
+		binningState.point_list_unsorted,
+		radii,
+		tile_grid)
+	CHECK_CUDA(, debug)
+
+	int bit = getHigherMsb(tile_grid.x * tile_grid.y);
+
+	CHECK_CUDA(cub::DeviceRadixSort::SortPairs(
+		binningState.list_sorting_space,
+		binningState.sorting_size,
+		binningState.point_list_keys_unsorted, binningState.point_list_keys,
+		binningState.point_list_unsorted, binningState.point_list,
+		num_rendered, 0, 32 + bit), debug)
+
+	CHECK_CUDA(cudaMemset(imgState.ranges, 0, tile_grid.x * tile_grid.y * sizeof(uint2)), debug);
+
+	if (num_rendered > 0)
+		identifyTileRanges << <(num_rendered + 255) / 256, 256 >> > (
+			num_rendered,
+			binningState.point_list_keys,
+			imgState.ranges);
+	CHECK_CUDA(, debug)
+
+	// Initialize top-k buffer to zero (the kernel does set it for valid pixels,
+	// but pixels outside the image still get written; safe to zero the whole thing).
+	CHECK_CUDA(cudaMemset(top_weights, 0, K * width * height * sizeof(float)), debug);
+
+	CHECK_CUDA(FORWARD::render_topk_weights(
+		tile_grid, block, K,
+		imgState.ranges,
+		binningState.point_list,
+		width, height,
+		geomState.means2D,
+		geomState.conic_opacity,
+		top_weights), debug)
+
+	return num_rendered;
+}
+
 // Produce necessary gradients for optimization, corresponding
 // to forward render pass
 void CudaRasterizer::Rasterizer::backward(
diff --git a/diff_gaussian_rasterization/__init__.py b/diff_gaussian_rasterization/__init__.py
index 7f228ce..ca3c443 100644
--- a/diff_gaussian_rasterization/__init__.py
+++ b/diff_gaussian_rasterization/__init__.py
@@ -171,8 +171,52 @@ class GaussianRasterizer(nn.Module):
             
         return visible
 
+    def rasterize_topk_weights(self, means3D, opacities, scales=None, rotations=None, cov3D_precomp=None, top_k=4):
+        """
+        Per-pixel top-K alpha-blending weights, sorted descending.
+
+        Returns (top_weights, radii) where top_weights has shape [K, H, W]
+        holding the K largest w_i = alpha_i * T_i seen for each pixel.
+
+        Inference-only: no backward, no SH/color path. K must be in {1, 2, 4, 8}.
+        """
+        raster_settings = self.raster_settings
+
+        if ((scales is None or rotations is None) and cov3D_precomp is None) or \
+           ((scales is not None or rotations is not None) and cov3D_precomp is not None):
+            raise Exception('Provide exactly one of scale/rotation pair or precomputed 3D covariance!')
+
+        if scales is None:
+            scales = torch.Tensor([])
+        if rotations is None:
+            rotations = torch.Tensor([])
+        if cov3D_precomp is None:
+            cov3D_precomp = torch.Tensor([])
+
+        with torch.no_grad():
+            num_rendered, top_weights, radii = _C.rasterize_gaussians_topk(
+                means3D,
+                opacities,
+                scales,
+                rotations,
+                raster_settings.scale_modifier,
+                cov3D_precomp,
+                raster_settings.viewmatrix,
+                raster_settings.projmatrix,
+                raster_settings.tanfovx,
+                raster_settings.tanfovy,
+                raster_settings.image_height,
+                raster_settings.image_width,
+                raster_settings.campos,
+                int(top_k),
+                raster_settings.prefiltered,
+                raster_settings.antialiasing,
+                raster_settings.debug,
+            )
+        return top_weights, radii
+
     def forward(self, means3D, means2D, opacities, shs = None, colors_precomp = None, scales = None, rotations = None, cov3D_precomp = None):
-        
+
         raster_settings = self.raster_settings
 
         if (shs is None and colors_precomp is None) or (shs is not None and colors_precomp is not None):
diff --git a/ext.cpp b/ext.cpp
index d768779..52a8a60 100644
--- a/ext.cpp
+++ b/ext.cpp
@@ -15,5 +15,6 @@
 PYBIND11_MODULE(TORCH_EXTENSION_NAME, m) {
   m.def("rasterize_gaussians", &RasterizeGaussiansCUDA);
   m.def("rasterize_gaussians_backward", &RasterizeGaussiansBackwardCUDA);
+  m.def("rasterize_gaussians_topk", &RasterizeGaussiansTopKCUDA);
   m.def("mark_visible", &markVisible);
 }
\ No newline at end of file
diff --git a/rasterize_points.cu b/rasterize_points.cu
index e625c19..6dc1ec8 100644
--- a/rasterize_points.cu
+++ b/rasterize_points.cu
@@ -226,11 +226,11 @@ torch::Tensor markVisible(
 		torch::Tensor& means3D,
 		torch::Tensor& viewmatrix,
 		torch::Tensor& projmatrix)
-{ 
+{
   const int P = means3D.size(0);
-  
+
   torch::Tensor present = torch::full({P}, false, means3D.options().dtype(at::kBool));
- 
+
   if(P != 0)
   {
 	CudaRasterizer::Rasterizer::markVisible(P,
@@ -239,6 +239,81 @@ torch::Tensor markVisible(
 		projmatrix.contiguous().data<float>(),
 		present.contiguous().data<bool>());
   }
-  
+
   return present;
 }
+
+std::tuple<int, torch::Tensor, torch::Tensor>
+RasterizeGaussiansTopKCUDA(
+	const torch::Tensor& means3D,
+	const torch::Tensor& opacity,
+	const torch::Tensor& scales,
+	const torch::Tensor& rotations,
+	const float scale_modifier,
+	const torch::Tensor& cov3D_precomp,
+	const torch::Tensor& viewmatrix,
+	const torch::Tensor& projmatrix,
+	const float tan_fovx,
+	const float tan_fovy,
+	const int image_height,
+	const int image_width,
+	const torch::Tensor& campos,
+	const int top_k,
+	const bool prefiltered,
+	const bool antialiasing,
+	const bool debug)
+{
+	if (means3D.ndimension() != 2 || means3D.size(1) != 3) {
+		AT_ERROR("means3D must have dimensions (num_points, 3)");
+	}
+	if (top_k <= 0) {
+		AT_ERROR("top_k must be positive");
+	}
+
+	const int P = means3D.size(0);
+	const int H = image_height;
+	const int W = image_width;
+	const int K = top_k;
+
+	auto float_opts = means3D.options().dtype(torch::kFloat32);
+	auto int_opts = means3D.options().dtype(torch::kInt32);
+
+	torch::Tensor top_weights = torch::zeros({K, H, W}, float_opts);
+	torch::Tensor radii = torch::full({P}, 0, int_opts);
+
+	torch::Device device(torch::kCUDA);
+	torch::TensorOptions options(torch::kByte);
+	torch::Tensor geomBuffer = torch::empty({0}, options.device(device));
+	torch::Tensor binningBuffer = torch::empty({0}, options.device(device));
+	torch::Tensor imgBuffer = torch::empty({0}, options.device(device));
+	std::function<char*(size_t)> geomFunc = resizeFunctional(geomBuffer);
+	std::function<char*(size_t)> binningFunc = resizeFunctional(binningBuffer);
+	std::function<char*(size_t)> imgFunc = resizeFunctional(imgBuffer);
+
+	int rendered = 0;
+	if (P != 0) {
+		rendered = CudaRasterizer::Rasterizer::forward_topk_weights(
+			geomFunc,
+			binningFunc,
+			imgFunc,
+			P, /*D*/ 0, /*M*/ 0, K,
+			W, H,
+			means3D.contiguous().data<float>(),
+			opacity.contiguous().data<float>(),
+			scales.contiguous().data_ptr<float>(),
+			scale_modifier,
+			rotations.contiguous().data_ptr<float>(),
+			cov3D_precomp.contiguous().data<float>(),
+			viewmatrix.contiguous().data<float>(),
+			projmatrix.contiguous().data<float>(),
+			campos.contiguous().data<float>(),
+			tan_fovx,
+			tan_fovy,
+			prefiltered,
+			top_weights.contiguous().data<float>(),
+			antialiasing,
+			radii.contiguous().data<int>(),
+			debug);
+	}
+	return std::make_tuple(rendered, top_weights, radii);
+}
diff --git a/rasterize_points.h b/rasterize_points.h
index 82cbd4f..a5f7a55 100644
--- a/rasterize_points.h
+++ b/rasterize_points.h
@@ -69,3 +69,25 @@ torch::Tensor markVisible(
 		torch::Tensor& means3D,
 		torch::Tensor& viewmatrix,
 		torch::Tensor& projmatrix);
+
+// Forward pass that, for each pixel, returns the K largest alpha-blending
+// weights w_i = alpha_i * T_i, sorted descending. Output shape: [K, H, W].
+std::tuple<int, torch::Tensor, torch::Tensor>
+RasterizeGaussiansTopKCUDA(
+	const torch::Tensor& means3D,
+	const torch::Tensor& opacity,
+	const torch::Tensor& scales,
+	const torch::Tensor& rotations,
+	const float scale_modifier,
+	const torch::Tensor& cov3D_precomp,
+	const torch::Tensor& viewmatrix,
+	const torch::Tensor& projmatrix,
+	const float tan_fovx,
+	const float tan_fovy,
+	const int image_height,
+	const int image_width,
+	const torch::Tensor& campos,
+	const int top_k,
+	const bool prefiltered,
+	const bool antialiasing,
+	const bool debug);
